const number1 = 20;
const number2 = 10;

if (number1 == number2) {
  console.log(
    `${number1} is equal and may not the data type is equal to ${number2}`
  );
}

if (number1 === number2) {
  console.log(`${number1} is equal to and the data type is same as ${number2}`);
}
