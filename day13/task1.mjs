export const addTwoNum = (a, b) => a + b;

export const person = {
  name: "Akash",
  age: 20,
  greet: function () {
    return `Hello, ${this.name}`;
  },
};

const welcome = () => `Welcome to this universe!`;

export default welcome;
